[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "wikirace"
version = "0.1.0"
dependencies = ["pyyaml"]

[tool.pytest.ini_options]
pythonpath = ["src"]
